steps:
  - task: Docker@2
    displayName: Build image
    inputs:
      command: build
      dockerfile: "$(Build.SourcesDirectory)/docker/$(imageName)/Dockerfile"
      tags: |
        $(imageName)
